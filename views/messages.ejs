


<div id="messages">
  <% Object.keys(locals.messages).forEach(function (type) { %>
    <% console.log("type: " + type) %>
    <% messages[type].forEach(function (message) { %>
      <% let t = type %>
        <div class='alert alert-info'+<%t%> role='alert'> <%=message-%></div>

    <% }) %>
  <% }) %>
</div>
